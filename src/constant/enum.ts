export enum AppearanceType {
  Light = "light",
  Dark = "dark",
}

export enum ResultType {
  FAIL = 0,
  SUCCESS,
}

export enum HttpStatusCode {
  OK = 200,
  CREATED = 201,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  INTERNAL_SERVER = 500,
}
